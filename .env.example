# Local development only. Copy to .env.local; never use Railway credentials here.
DATABASE_URL="postgresql://leaflog:leaflog@localhost:5437/leaf_log_dev?schema=public"
# Generate with: node -e "console.log(require('node:crypto').randomBytes(32).toString('hex'))"
AUTH_SECRET=""
AUTH_URL="http://localhost:3000"
NEXTAUTH_URL="http://localhost:3000"
AUTH_EMAIL_FROM="Leaf Log <noreply@leaflog.local>"
# Leave unset to log sign-in links locally instead of sending email.
RESEND_API_KEY=""
